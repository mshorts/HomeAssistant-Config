purge_keep_days: 10
purge_interval: 1
db_url: !secret db_url
exclude:
  domains:
    - weblink
    - updater
    - group
  entities:
    - sensor.last_boot
    - sensor.since_last_boot
    - sensor.date
    - sun.sun
